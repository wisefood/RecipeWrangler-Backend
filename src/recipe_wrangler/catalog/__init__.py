"""Catalog layer — Elasticsearch-backed entities for recipes and profiles.

Structure mirrors wisefood-data-api's paradigm (URN identity, declarative index
schemas, an entity layer that owns validation and system fields, one uniform
search contract) with the deviations documented per module.
"""
